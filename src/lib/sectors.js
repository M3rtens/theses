// The canonical set of sectors a thesis can be tagged with. Shared so the
// Editor (where authors pick a sector) and Discover (where readers filter by
// one) always offer the same list and never drift apart.
export const SECTORS = [
  'Consumer',
  'Energy',
  'Financials',
  'Healthcare',
  'Semiconductors',
  'Software',
]
